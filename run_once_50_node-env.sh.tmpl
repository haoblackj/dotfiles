#!/bin/bash
set -eu

# nvm はシェル関数のため lookPath では検出できない。ディレクトリで確認する
if [ -s "$HOME/.nvm/nvm.sh" ]; then
  echo "nvmインストール済"
else
  echo "nvmインストール開始"
  curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.7/install.sh | bash
fi
